@* 员工列表 通过页面控制器传入 filter姓名过滤 和 departmentid id过滤 直接产生列表 *@
@model List<Employee>

@if (Model is not null && Model.Count() > 0)
{
	@foreach (var emp in Model)
	{
		@await Html.PartialAsync("_EmployeeListItem", emp)
	}
}
